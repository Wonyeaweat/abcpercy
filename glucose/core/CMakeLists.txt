add_library(glucose "Solver.cc" "../simp/SimpSolver.cc")
target_include_directories(glucose PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/../..)
target_include_directories(glucose PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/..)
